# Optional: bake in the deployed cloud-sync Worker URL so users don't
# have to paste it. Leave unset to enter the endpoint in Settings.
# See worker/README.md to deploy the Worker.
VITE_SYNC_ENDPOINT=
