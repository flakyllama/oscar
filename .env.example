# Optional. The sync API ships with the app, so same-origin is the
# default and this can stay empty. Set it only if you host the API
# somewhere other than the app (e.g. pointing local dev at production).
VITE_SYNC_ENDPOINT=
